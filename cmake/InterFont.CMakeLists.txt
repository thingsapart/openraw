# This is a shim CMakeLists.txt to make the Inter font download
# compatible with FetchContent_MakeAvailable. It doesn't build anything.
cmake_minimum_required(VERSION 3.16)
project(InterFont-wrapper)
